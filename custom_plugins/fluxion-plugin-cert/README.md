# Fluxion - Cert Integration Engine
Adds an asynchronous SSL/TLS certificate evaluation and diagnostic engine into the Fluxion core.

### Install
Directly install it into the isolated Fluxion environment:
```bash
pip install -e ./fluxion-plugin-cert
```
