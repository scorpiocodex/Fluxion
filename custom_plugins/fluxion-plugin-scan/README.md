# Fluxion - Port Scan Engine
Adds an asynchronous, high-speed TCP socket matrix scanner into the Fluxion core.

### Install
Directly install it into the isolated Fluxion environment:
```bash
pip install -e ./fluxion-plugin-scan
```
